import { Router } from "express";

const router = Router();

router.get("/recipes", );
router.get("/recipes/:id", );
router.post("/recipes/create", );
router.post("/recipes/:id/delete", );
router.post("/recipes/:id/update", );

export default router;